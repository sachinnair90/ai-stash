# Override the registry URL for local development
# Copy this file to .env and set your values
REGISTRY_URL=https://raw.githubusercontent.com/DevOnGlobal/devon-ai-sdlc-registry/main/registry.json

# GitHub token is resolved automatically via `gh auth token`.
# Only set this if you need to override it explicitly.
# GITHUB_TOKEN=
